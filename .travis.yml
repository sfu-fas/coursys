language: python
python:
  - "2.7"
notifications:
    email:
      - coursys-help@sfu.ca
cache: pip
sudo: required
services:
  - docker
install: "docker build -t django-coursys -f machines/docker/Dockerfile ."
script: "docker run django-coursys"